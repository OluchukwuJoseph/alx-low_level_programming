#include <stdio.h>

/**
 * main - Entry point
 *
 * Description - This is the standard entry point (Beginning) of all C programs
 * Return: Displays the output of Arguments passed
 */
int main(void)
{
/*sets s as a variable with string data type*/
char s[] = "Programming is like building a multilingual puzzle";
/*Prints s*/
puts(s);
return (0);
}
